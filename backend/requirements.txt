# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
python-multipart>=0.0.12
pydantic>=2.10.2
pydantic-settings>=2.6.1

# ── LangChain RAG stack ───────────────────────────────────────────────────────
langchain>=0.3.9
langchain-community>=0.3.9
langchain-huggingface>=0.1.2
langchain-chroma>=0.1.4
langchain-openai>=0.2.11

# ── Vector store ──────────────────────────────────────────────────────────────
# chromadb 1.5+ ships cp39-abi3 wheels → fully compatible with Python 3.13
chromadb>=1.5.0

# ── Embeddings (multilingual EN+ID) ───────────────────────────────────────────
# sentence-transformers 5.x requires Python 3.10+ — works fine on 3.13
sentence-transformers>=5.0.0
transformers>=4.47.1

# ── PyTorch CPU ───────────────────────────────────────────────────────────────
# Install separately FIRST with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then run: pip install -r requirements.txt
torch>=2.5.1

# ── PDF ingestion ─────────────────────────────────────────────────────────────
pypdf>=5.1.0
pymupdf>=1.24.13

# ── OpenRouter LLM (OpenAI-compatible API) ────────────────────────────────────
openai>=1.57.2
httpx>=0.28.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
tenacity>=9.0.0
loguru>=0.7.3

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.4
pytest-asyncio>=0.24.0
